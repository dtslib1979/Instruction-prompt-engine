# Instruction Prompt Engine Configuration

# GitHub Repository Settings (used by PWA for dynamic content loading)
REPO_OWNER=dtslib1979
REPO_NAME=Instruction-prompt-engine
REPO_BRANCH=main

# Application Settings
APP_VERSION=v24.1
CACHE_VERSION=v24-1

# Development Settings
DEV_PORT=8080
NODE_ENV=development

# Optional: Analytics or monitoring (if implemented)
# ANALYTICS_ID=
# SENTRY_DSN=

# Note: This PWA is primarily client-side and doesn't require many environment variables
# Most configuration is done through config/settings.json and config/favorites.json